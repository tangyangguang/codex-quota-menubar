# Codex 额度 · 使用说明（给朋友）

一个 macOS 菜单栏小工具，在菜单栏显示你 Codex 本周剩余额度。

## 运行前提

1. **必须已安装并登录 ChatGPT 桌面版（或 Codex CLI）**
   本工具不自带数据，它读取你本机已登录的 Codex 额度。如果没装、没登录，会提示
   “未检测到 Codex”。它只读，不碰你的账号密码或令牌。
2. macOS 14 及以上。
3. Intel 和 Apple Silicon（M 系列）Mac 都支持。

## 安装步骤

1. 把收到的 `Codex 额度.app` 拖到「应用程序」文件夹（或任意位置）。
2. **第一次打开会被系统拦一下**（因为这是免费本地签名，不是 App Store 应用，
   这是正常现象，不是病毒）：
   - **右键点击** App 图标 → 选「打开」→ 在弹窗里再点一次「打开」。
   - 如果提示「已损坏，无法打开」，打开「终端」执行下面这行（把路径换成 App 实际位置）：
     ```sh
     xattr -cr "/Applications/Codex 额度.app"
     ```
     然后再右键「打开」。
   - 或者：系统设置 → 隐私与安全性 → 找到被拦的「Codex 额度」→ 点「仍要打开」。
3. 打开后，菜单栏（屏幕右上角）会出现一个终端图标 + 剩余百分比。

## 使用

- **点菜单栏图标**：立即刷新并打开详情面板，显示剩余额度、每周重置时间、最后更新时间。
- **齿轮 ⚙️ 按钮**：进入设置，可选自动刷新间隔（30秒 / 1分钟 / 2分钟 / 3分钟 / 5分钟），
  选中即生效，重启后仍记得你的选择。
- **退出**：面板右下角的「退出」按钮。

## 说明

- 电脑休眠时自动暂停刷新，唤醒后自动恢复，几乎不占资源。
- 全部在本机运行，不联网上传任何数据。
